# Environment-specific configurations, allowing different base URLs or settings for different environments (e.g., default, dev, qa).
environments {
  # Default environment settings.
  default {
    # The base URL for API requests in the default environment.
    #base.url = "https://reqres.in/api/test-suite"
    base.url = "https://reqres.in/api"
  }
}